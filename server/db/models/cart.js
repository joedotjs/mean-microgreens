// var mongoose = require ('mongoose');

// var cartSchema = new mongoose.Schema({
// 	blend: [{type: Schema.Types.ObjectId, ref: 'Blend'}],
// 	quantity: {type: Number}
// })

// var Cart = mongoose.model('Cart', cartSchema);

// module.export = Cart;